let x = 10;
{
  let y = 20;

}

console.log("Fuera del bloque: x =", x);
console.log("Fuera del bloque: y =", y);

// cuando el let esta dentro de un bloque, no se puede acceder a el desde fuera del bloque, por eso da error//